<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:VerkehrsControls">
  <!--<ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="..."/>
  </ResourceDictionary.MergedDictionaries>-->
  <Style TargetType="{x:Type local:Ampel}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type local:Ampel}">
          <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
            <StackPanel>
              <Ellipse Name="PART_LampeRot" Width="50" Height="50" Fill="Red" Margin="5"/>
              <Ellipse Name="PART_LampeGrün" Width="50" Height="50" Fill="#FF37F731" Margin="5"/>
            </StackPanel>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
